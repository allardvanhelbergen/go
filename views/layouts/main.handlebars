<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css">
    <link rel="stylesheet" type="text/css" href="/css/style.css">

    <title>{{config.app.TITLE}} - {{config.app.DESCRIPTION}}</title>
</head>
<body>
    <nav class="navbar navbar-inverse navbar-static-top" role="navigation">
        <div class="container">
            <div class="navbar-header">
                <a class="navbar-brand" href="/">{{config.app.TITLE}}</a>
            </div>
            {{#if user}}
                <div class="collapse navbar-collapse">
                    <ul class="nav navbar-nav navbar-right">
                        <li class="dropdown">
                            <a href="#" class="navbar-avatar-link" data-toggle="dropdown">
                                <img class="navbar-avatar-image img-circle" src="{{user._json.picture}}" alt="">
                                <span class="caret"></span>
                            </a>
                            <ul class="dropdown-menu">
                                <li>
                                    <a href="/auth/logout">
                                        <span class="glyphicon glyphicon-log-out"></span>
                                        Log out
                                    </a>
                                </li>
                            </ul>
                        </li>
                    </ul>
                </div>
            {{/if}}
        </div>
    </nav>

    <div class="container">
        {{#if flash.success}}
            <div class="row">
                <div class="col-lg-8 col-center">
                    <div class="alert alert-success" role="alert">
                        <p>{{flash.success}}</p>
                    </div>
                </div>
            </div>
        {{/if}}
        {{{body}}}
    </div>

    <footer class="navbar navbar-default navbar-fixed-bottom">
        <div class="container">
            <div class="row">
                <div class="col-lg-6 col-sm-6">
                    <p class="text-muted navbar-text">
                        <a href="/">{{config.app.TITLE}}</a>
                        is a Funky Friday project by
                        <a href="{{config.app.AUTHOR_URL}}">{{config.app.AUTHOR_LDAP}}@</a>
                    </p>
                </div>
                <div class="col-lg-6 col-sm-6">
                    <p class="text-muted navbar-right navbar-text">
                        <a href="{{config.app.ISSUE_URL}}">Report a bug</a>
                    </p>
                </div>
            </div>
        </div>
    </footer>

    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.2/jquery.min.js"></script>
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.3.2/js/bootstrap.min.js"></script>
    <script src="/js/google-analytics.js" type="text/javascript" charset="utf-8" async defer></script>
</body>
</html>
